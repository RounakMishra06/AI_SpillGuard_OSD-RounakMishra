
# Deployment requirements (pip format). This file is used by Streamlit Cloud / Docker.
# Notes:
# - Torch and torchvision are pinned to tested CPU-compatible versions.
# - If you host the model externally, the app will download it at runtime (requires 'requests' or 'gdown').

streamlit>=1.28.0
torch==2.0.1
torchvision==0.15.2
opencv-python-headless==4.8.1.78
Pillow==10.0.0
numpy==1.24.3
matplotlib==3.7.2
requests>=2.28.0
gdown>=4.7.0

# Optional (uncomment if you need):
# albumentations>=1.3.0
# pandas>=1.5.0
